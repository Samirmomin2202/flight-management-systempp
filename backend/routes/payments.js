// PayPal routes removed. This module is intentionally a no-op to keep imports harmless if referenced.
import express from "express";
const router = express.Router();

// All PayPal endpoints have been disabled/removed. Keep this empty router to avoid import errors.

export default router;

